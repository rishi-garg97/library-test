{
  "common": {
    "required": "{{fieldName}} is required.",
    "minLength": "{{fieldName}} has minimum {{minLength}} characters.",
    "maxLength": "{{fieldName}} has maximum {{maxLength}} characters.",
    "min": "{{fieldName}} has minimum value {{min}}",
    "max": "{{fieldName}} has maximum value {{max}}",
    "email": "{{fieldName}} is invalid.",
    "mobile": "{{fieldName}} is invalid.",
    "pattern": "{{fieldName}} has pattern {{pattern}}."
  },
  "fields": {
    "protocolName": {
      "minLength": "{{fieldName}} has minimum {{minLength}} characters.",
      "maxLength": "{{fieldName}} has maximum {{maxLength}} characters."
    },
    "amountOnColumn": {
      "required": "{{fieldName}} is required."
    },
    "columnLength": {
      "required": "{{fieldName}} is required."
    },
    "columnInnerDiameter": {
      "required": "{{fieldName}} is required."
    },
    "sampleSubmitterId": {
      "required": "{{fieldName}} is required."
    },
    "mobile": {
      "required": "{{fieldName}} is required."
    }
  }
}
